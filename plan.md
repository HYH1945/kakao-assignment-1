[개요]
초기 프로젝트 구조는 vanilla-js로 app.js, index.html, plan.md, style.css가 구현되어 있다. (assignment-2 폴더는 Vite로 React 프로젝트를 초기 설정한 상태)
프로젝트 내용은 todo 서비스 구현인데, 구현된 기능은 [구현된기능요약]을 참고
이번에 할 것은 이렇게 구현되어있는 TODO앱을 React Function Component 구조로 변환(마이그레이션)하는 것이다.

[구현된기능요약]
- TODO CRUD (Create, Read, Update, Delete)
- 상태별 필터링 (전체, 진행중, 완료 필터 탭을 통해 원하는 Todo만 볼 수 있음)
- 날짜별로 TODO를 관리할 수 있음 (현재는 일간뷰에서 확장되서 주간뷰로 구현되어 있음)
- 로컬 스토리지에 연동되어 변경사항 저장

[조건]
- 코드에는 동작 방식을 이해할 수 있도록 주석을 달아줘
- 기능을 추가하거나 수정할 때는 **관련된 React 컴포넌트 파일(예: App.jsx, components/TodoList.jsx 등)**의 코드를 중심으로 보여주고, 컴포넌트 분리 기준을 명확히 설명해 줘
- 모든 작업을 마친 후에는 어떤 기능을 구현했는지 체크리스트로 답변해

[활용스택]
- React (v18+), Vite (v5.x), Tailwind CSS(v4.x), JavaScript, Web Storage API (localStorage)

[과제목표]
1. Todo CRUD (Create, Read, Update, Delete) 기능 마이그레이션
- 텍스트 입력창과 추가 버튼으로 새로운 Todo를 생성
- 입력값이 비어있는 경우 Todo가 생성되지 않고, 사용자에게 안내 메시지를 표시
- 생성된 Todo는 목록 형태로 화면에 표시
- 각 Todo 항목은 수정 / 완료 처리 / 삭제가 가능
- 완료 처리된 Todo는 시각적으로 구분 (ex. 텍스트에 취소선 표시)
- `prompt()` 대신 인라인 입력창으로 수정 UI를 구현

2. 상태별 필터링 기능 마이그레이션
- 전체 / 진행 중 / 완료 필터 탭을 통해 원하는 Todo만 볼 수 있음
- 현재 선택된 필터 탭은 시각적으로 구분됨
- 필터 상태는 `useState`로 관리하고, 탭 전환 후 새 Todo를 추가해도 필터가 유지됨

3. Todo 일간 뷰 마이그레이션
- 화면에 오늘 날짜가 표시되고, 이전 / 다음 날짜로 이동할 수 있음
- 선택된 날짜에 해당하는 Todo만 표시됨
- Todo를 생성하면 현재 선택된 날짜가 자동으로 저장
- 선택된 날짜 상태는 `useState`로 관리

4. 로컬 스토리지 연동 마이그레이션
- Todo의 변경사항은 항상 로컬스토리지에 저장
- 새로고침 후에도 기존 데이터가 유지
- 데이터는 JSON 형태로 저장하고 불러옴 ( `JSON.stringify` / `JSON.parse` )
- `useEffect`를 활용해 todos 변경 시 자동으로 저장

5. Todo 주간 뷰 마이그레이션
- 이번 주 날짜 목록을 표시하고, 날짜를 선택하면 해당 날짜의 Todo만 볼 수 있음
- 이전 주차 / 다음 주차로 넘길 수 있는 버튼이 있음
- 각 날짜 아래에 해당 날짜의 Todo 개수를 표시
- 오늘 날짜는 시각적으로 구분되는 스타일을 적용
- 주간 뷰 상태는 `useState`로 관리하고, 새로고침 후에도 유지